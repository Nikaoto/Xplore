<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/nav_profile"
        android:title="@string/profile"
        android:icon="@drawable/ic_profile"/>

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_library"
            android:title="@string/nav_lib_title"
            android:icon="@drawable/ic_book"/>
    </group>

    <item
        android:id="@+id/nav_map"
        android:title="@string/nav_map_title"
        android:icon="@drawable/ic_map"/>

    <item android:title="@string/groups">
        <menu>
            <group android:checkableBehavior="single">
                <item
                    android:id="@+id/nav_my_groups"
                    android:title="@string/my_groups"
                    android:icon="@drawable/ic_my_groups"/>
                <item
                    android:id="@+id/nav_find_create_groups"
                    android:title="@string/find_create"
                    android:icon="@drawable/ic_find_create_groups" />
            </group>
        </menu>
    </item>

    <group
        android:id="@+id/nav_settings_group"
        android:checkableBehavior="single">
        <item
            android:id="@+id/nav_settings"
            android:title="@string/settings"
            android:icon="@drawable/ic_settings"/>
    </group>

</menu>
